"""LLM provider routing for Photo Agents.

The :mod:`photoagents.llm.router` module owns the multi-provider session
classes (Anthropic native, OpenAI native, mixin failover) and the
credential-loading machinery.
"""
