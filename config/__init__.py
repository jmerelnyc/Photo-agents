"""Photo Agents user configuration helpers.

The :mod:`photoagents.auth.license` module owns the on-disk config file at
``~/.photoagents/config.json``. This package only exposes the LLM-credential
template that users copy to ``credentials.py`` (or ``credentials.json``) at
the project root.
"""
